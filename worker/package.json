{
  "name": "trofia-ai-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:node && npm run test:runtime",
    "test:node": "node --test ../tests/unit/ai-worker.test.js ../tests/unit/firebase-id-token.test.js",
    "test:runtime": "vitest run",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "jose": "6.2.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.19.0",
    "vitest": "4.1.10",
    "wrangler": "4.115.0"
  }
}
